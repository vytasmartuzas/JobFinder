"""JobFinder — a human-in-the-loop job-application assistant.

The package is framework-agnostic core logic. The Streamlit app (and, later, a
FastAPI/React frontend) imports from here; business logic never lives in the UI.
"""

__version__ = "0.1.0"
